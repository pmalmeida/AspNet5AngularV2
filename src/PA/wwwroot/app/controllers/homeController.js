(function () {
    appControllers.controller('homeController',
    function controller($scope) {
        $scope.title = 'homeController';

        $scope.name = 'xpto';

        activate();

        function activate() { }
    });
})();
